# 阶段 H：因子研究

## 阶段目标

新增因子、数据集、处理、评估、多因子和 LEAN 接入。

## 去歧义执行规则

- 本文件中的“必须”表示不可跳过。
- 本文件中的“不得”表示禁止实现。
- 未写明的框架、字段、阈值或算法不得由 Codex 自行新增；需要时先记录问题并请求人工决定。
- 简单阶段使用一个分支；复杂阶段仅按本文件列出的分支任务包拆分。

## H-BR1 分支任务包

- **工作分支**：`feature/H-factor-research-core`
- **人工门禁**：高风险：时间语义和未来数据防护需人工确认
- **状态来源**：`TASKS/INDEX.md`

## 分支级 Loop Engineering

1. 同步最新 `main`，创建本任务包指定分支。
2. 只按本文件中的任务顺序执行。
3. 每完成一个任务：
   - 运行相关测试；
   - 修复全部失败；
   - 更新任务状态；
   - 创建一次清晰 commit。
4. 不为分支内每个任务创建新分支或新 PR。
5. 分支全部任务完成后运行：
   - `make check`；
   - 需要时运行 `make test-integration`；
   - 需要时运行 `make e2e`。
6. 创建一个 PR。CI 失败时继续在原分支修复。
7. 按人工门禁规则合并。
8. 合并后停止，提示用户验收。
9. 只有用户回复“验收通过，继续”后，才将任务包标记 DONE 并领取下一项 READY。
10. 不允许因用户长时间未回复而自动视为验收通过。


### H0：因子、数据集和计算任务

#### 工作内容

- 本阶段才新增 Factor、Dataset、版本和快照。
- 复用 Job、Worker、MinIO 和 data_snapshots。
- 不得依赖 run_metadata 代替正式关系。

#### 任务验收

- 迁移和版本通过。
- 相关测试通过后创建一次 commit。

### H1：处理与评估

#### 工作内容

- 实现去极值、标准化、中性化、IC、Rank IC、ICIR、分层、衰减和相关性。
- 明确观察、发布时间、系统可用时间和收益区间。

#### 任务验收

- 无未来数据测试通过。
- 相关测试通过后创建一次 commit。

### 分支任务包完成条件

- 本任务包内全部任务完成。
- 完整检查通过。
- PR 描述包含范围、测试、风险、迁移、回滚和未完成项。
- 按门禁规则完成合并。
- 用户验收通过后才能领取下一任务包。

## H-BR2 分支任务包

- **工作分支**：`feature/H-factor-lean-ui`
- **人工门禁**：高风险：目标权重和 LEAN 接入需人工确认
- **状态来源**：`TASKS/INDEX.md`

## 分支级 Loop Engineering

1. 同步最新 `main`，创建本任务包指定分支。
2. 只按本文件中的任务顺序执行。
3. 每完成一个任务：
   - 运行相关测试；
   - 修复全部失败；
   - 更新任务状态；
   - 创建一次清晰 commit。
4. 不为分支内每个任务创建新分支或新 PR。
5. 分支全部任务完成后运行：
   - `make check`；
   - 需要时运行 `make test-integration`；
   - 需要时运行 `make e2e`。
6. 创建一个 PR。CI 失败时继续在原分支修复。
7. 按人工门禁规则合并。
8. 合并后停止，提示用户验收。
9. 只有用户回复“验收通过，继续”后，才将任务包标记 DONE 并领取下一项 READY。
10. 不允许因用户长时间未回复而自动视为验收通过。


### H2：多因子、LEAN 接入和页面

#### 工作内容

- 实现多因子和版本化权重。
- 建立正式 factor/dataset 外键关系。
- 由 LEAN 完成回测。

#### 任务验收

- 因子到 LEAN 闭环通过。
- 相关测试通过后创建一次 commit。

### H3：因子阶段验收

#### 工作内容

- 验证快照、版本、未来数据防护和复现。

#### 任务验收

- Phase H 验收通过。
- 相关测试通过后创建一次 commit。

### 分支任务包完成条件

- 本任务包内全部任务完成。
- 完整检查通过。
- PR 描述包含范围、测试、风险、迁移、回滚和未完成项。
- 按门禁规则完成合并。
- 用户验收通过后才能领取下一任务包。
