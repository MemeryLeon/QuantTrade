# 阶段 I：机器学习研究

## 阶段目标

新增 Feature、时间切分、Walk-forward、实验、Model Registry 和 LEAN 推理。

## 去歧义执行规则

- 本文件中的“必须”表示不可跳过。
- 本文件中的“不得”表示禁止实现。
- 未写明的框架、字段、阈值或算法不得由 Codex 自行新增；需要时先记录问题并请求人工决定。
- 简单阶段使用一个分支；复杂阶段仅按本文件列出的分支任务包拆分。

## I-BR1 分支任务包

- **工作分支**：`feature/I-ml-research-core`
- **人工门禁**：高风险：数据泄漏和时间切分需人工确认
- **状态来源**：`TASKS/INDEX.md`

## 分支级 Loop Engineering

1. 同步最新 `main`，创建本任务包指定分支。
2. 只按本文件中的任务顺序执行。
3. 每完成一个任务：
   - 运行相关测试；
   - 修复全部失败；
   - 更新任务状态；
   - 创建一次清晰 commit。
4. 不为分支内每个任务创建新分支或新 PR。
5. 分支全部任务完成后运行：
   - `make check`；
   - 需要时运行 `make test-integration`；
   - 需要时运行 `make e2e`。
6. 创建一个 PR。CI 失败时继续在原分支修复。
7. 按人工门禁规则合并。
8. 合并后停止，提示用户验收。
9. 只有用户回复“验收通过，继续”后，才将任务包标记 DONE 并领取下一项 READY。
10. 不允许因用户长时间未回复而自动视为验收通过。


### I0：Feature 与训练数据集

#### 工作内容

- 本阶段才新增 Feature、Model 和 Experiment 表。
- Feature 绑定数据集、版本和系统可用时间。
- 训练、验证和测试必须按时间切分。

#### 任务验收

- 数据泄漏检查通过。
- 相关测试通过后创建一次 commit。

### I1：Walk-forward、训练与实验

#### 工作内容

- 实现滚动窗口。
- 受控支持 Scikit-learn、LightGBM 和 XGBoost。
- 记录代码、数据、特征、参数、指标、随机种子和产物。

#### 任务验收

- 训练可复现。
- 相关测试通过后创建一次 commit。

### 分支任务包完成条件

- 本任务包内全部任务完成。
- 完整检查通过。
- PR 描述包含范围、测试、风险、迁移、回滚和未完成项。
- 按门禁规则完成合并。
- 用户验收通过后才能领取下一任务包。

## I-BR2 分支任务包

- **工作分支**：`feature/I-model-registry-lean`
- **人工门禁**：高风险：模型准入和 LEAN 推理需人工确认
- **状态来源**：`TASKS/INDEX.md`

## 分支级 Loop Engineering

1. 同步最新 `main`，创建本任务包指定分支。
2. 只按本文件中的任务顺序执行。
3. 每完成一个任务：
   - 运行相关测试；
   - 修复全部失败；
   - 更新任务状态；
   - 创建一次清晰 commit。
4. 不为分支内每个任务创建新分支或新 PR。
5. 分支全部任务完成后运行：
   - `make check`；
   - 需要时运行 `make test-integration`；
   - 需要时运行 `make e2e`。
6. 创建一个 PR。CI 失败时继续在原分支修复。
7. 按人工门禁规则合并。
8. 合并后停止，提示用户验收。
9. 只有用户回复“验收通过，继续”后，才将任务包标记 DONE 并领取下一项 READY。
10. 不允许因用户长时间未回复而自动视为验收通过。


### I2：Model Registry 与 LEAN 推理

#### 工作内容

- 实现模型状态、版本、指标、依赖和审批。
- 建立正式 model_version 和 dataset 关系。
- 未经批准不得进入交易。

#### 任务验收

- 模型回测可复现。
- 相关测试通过后创建一次 commit。

### I3：研究页面与最终验收

#### 工作内容

- 展示数据、特征、实验、模型和回测。
- 不得跳过验证进入 Live。

#### 任务验收

- Phase I 验收通过。
- 相关测试通过后创建一次 commit。

### 分支任务包完成条件

- 本任务包内全部任务完成。
- 完整检查通过。
- PR 描述包含范围、测试、风险、迁移、回滚和未完成项。
- 按门禁规则完成合并。
- 用户验收通过后才能领取下一任务包。
